import { Box, Typography } from '@material-ui/core'

const Logo = () => (
  <Box flexGrow={1}>
    <Typography variant="h6" color="textSecondary">
      kiselev.dev
    </Typography>
  </Box>
)

export default Logo
